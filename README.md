# 🚗 معرض السيارات الذكي (Smart Product Previewer)

تطبيق ويب مبني باستخدام React و TypeScript، يستخدم الذكاء الاصطناعي (Gemini AI) لاستخراج تفاصيل السيارات والمنتجات تلقائياً من روابط موقع Avito.ma.

## ✨ المميزات
- استخراج تلقائي للبيانات (العنوان، السعر، الصور، الوصف).
- التعرف على أرقام الهواتف وروابط الواتساب.
- ميزة "المسودة" لمراجعة الإعلان وتعديل صوره قبل النشر.
- منع الروابط غير المدعومة (حصري لـ Avito).
- تصميم متجاوب (Grid System) يعمل على الهواتف والحواسيب.

## 🚀 طريقة التشغيل (محلياً)

1. **تثبيت المكتبات:**
   ```bash
   npm install
   ```

2. **إعداد مفتاح API:**
   - قم بإنشاء ملف جديد باسم `.env` في المجلد الرئيسي.
   - أضف مفتاح Gemini API الخاص بك بداخله كالتالي:
     ```
     VITE_GEMINI_API_KEY=AIzaSy... (مفتاحك هنا)
     ```

3. **تشغيل المشروع:**
   ```bash
   npm run dev
   ```

## 📦 الرفع على GitHub
1. أنشئ مستودع جديد (Repository) على GitHub.
2. نفذ الأوامر التالية في مجلد المشروع:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/USERNAME/REPO_NAME.git
   git push -u origin main
   ```
